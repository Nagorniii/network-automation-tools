version: "3.9"

services:
  monitor:
    build: .
    container_name: network_monitor
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
    working_dir: /app
    command: python main.py
    restart: unless-stopped
