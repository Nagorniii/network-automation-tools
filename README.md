# 🧠 Network Automation Tools

Набір інструментів для автоматизації роботи з мережевими пристроями: резервне копіювання конфігурацій, моніторинг, інтеграція з NetBox та інші задачі адміністрування.

---

## 🚀 Основна ідея

Проєкт створений для спрощення щоденних операцій мережевого інженера:
- автоматизація SSH-підключень до пристроїв;
- збирання конфігурацій;
- перевірка стану інтерфейсів і тунелів;
- інтеграція з **NetBox** для синхронізації даних про обладнання;
- формування звітів для подальшої обробки чи відображення на веб-інтерфейсі.

---

## 📂 Структура проєкту

```
network-automation-tools/
├── monitoring/          # Скрипти моніторингу стану пристроїв
├── netbox/              # Модулі взаємодії з API NetBox
├── ssh_backup/          # Резервне копіювання конфігурацій через SSH
├── .env.example         # Приклад конфігураційного файлу
└── main.py              # Точка входу в застосунок
```

---

## ⚙️ Використані технології

- **Python 3.10+**
- **Paramiko** — SSH-підключення до пристроїв  
- **Requests** — робота з API (NetBox)  
- **dotenv** — керування змінними середовища  
- **CSV / JSON** — для зберігання результатів  
- **Docker** (опціонально)

---

## 🧩 Приклади сценаріїв

### 🔹 Резервне копіювання конфігурацій
Скрипт з `ssh_backup` автоматично підключається до списку IP-адрес із файлу, зчитує конфігурації та зберігає їх локально у форматі `.txt`.

### 🔹 Моніторинг стану тунелів
Модуль `monitoring` виконує перевірку доступності інтерфейсів та тунелів, відображає результат на сайті.

### 🔹 Інтеграція з NetBox
Модуль `netbox` отримує дані через API — дозволяє автоматично оновлювати IP-адреси, опис або статуси обладнання.

---

## 🖥️ Як запустити

1. Клонувати репозиторій:
   ```bash
   git clone https://github.com/Nagorniii/network-automation-tools.git
   cd network-automation-tools
   ```

2. Створити віртуальне середовище:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. Встановити залежності:
   ```bash
   pip install -r requirements.txt
   ```

4. Створити `.env` файл на основі прикладу:
   ```bash
   cp .env.example .env
   ```

5. Запустити основний скрипт:
   ```bash
   python main.py
   ```

---

## 🧾 Приклад `.env` файлу

```bash
user=admin
pass=your_password
netbox_token=xxxxxxxxxxxx
netbox_url=http://netbox.local/api/
```

---

## 📊 Майбутні плани

- Додати web-панель з фільтрацією результатів
- Інтеграція з Telegram/Email для повідомлень
- Docker-композиція для швидкого розгортання
- Асинхронна обробка кількох пристроїв одночасно

---

## 👤 Автор

**Євгеній Nagornii**  
[GitHub профіль](https://github.com/Nagorniii)  

---

## 🪪 Ліцензія

Цей проєкт розповсюджується за ліцензією [MIT](LICENSE).

---

> 💡 Якщо цей проєкт здається цікавим — залиш зірочку ⭐ на GitHub або зроби fork.  
> Це допоможе розвитку інструменту та покаже, що він корисний спільноті.
